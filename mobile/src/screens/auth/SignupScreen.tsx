import { StyleSheet } from 'react-native';
import { AppText, ScreenContainer } from '../../components';

export function SignupScreen() {
  return (
    <ScreenContainer centered>
      <AppText variant="title">Signup</AppText>
    </ScreenContainer>
  );
}

const styles = StyleSheet.create({});
